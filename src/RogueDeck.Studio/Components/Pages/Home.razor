@page "/"

<PageTitle>RogueDeck Studio</PageTitle>

<h1>RogueDeck Studio</h1>
<p class="muted">
    One sandbox for the whole engine. Author and play a single fight in the <a href="combat">Combat</a>
    sandbox, or drive a whole run — events, encounters, rewards, relics — in the <a href="run">Run</a> sandbox.
    The standalone combat sandbox (RogueDeck.Sandbox) still runs on its own; this app reuses the same UI.
</p>
